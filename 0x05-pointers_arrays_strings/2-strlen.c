#include "main.h"

/**
 * _strlen - Returns the length of a string.
 * @str: string.
 *
 * Return: Length
 */

int _strlen(const char *str)
{
	 int len = 0;

	while (*s != '\0')
		len++;
		str++;

	return (len);
}
